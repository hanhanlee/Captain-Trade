# 使用說明手冊索引

- 文件: `台股交易輔助工具 — 完整使用說明手冊`
- 路徑: `E:\workspace\srock tool\使用說明手冊.md`
- 版本: `2.2`
- 最後更新: `2026-04-18`
- 總行數: `2451`
- 章節數: `19`

## 使用方式

- 先讀這份 index，確認要看的章節標題與行號。
- 只擷取需要的章節，不要整份手冊一起讀。
- 更新手冊後重新執行 scripts/build_manual_index.py。

## 章節索引

- L1-L9 `台股交易輔助工具 — 完整使用說明手冊` — **版本：** 2.2　　**最後更新：** 2026-04-18
- L10-L45 `目錄` — **第一部分：認識這套工具**
- L46-L221 `第一部分：認識這套工具` — **這套工具是「輔助決策系統」，不是自動下單機器人。**
  - L50-L69 `第一章：這套工具能幫你做什麼` — **這套工具是「輔助決策系統」，不是自動下單機器人。**
    - L52-L57 `工具定位` — **這套工具是「輔助決策系統」，不是自動下單機器人。**
    - L58-L69 `五大核心問題，工具給你答案` — | 你的問題 | 工具的回答 | 對應功能 |
  - L70-L103 `第二章：設計哲學與核心理念` — 本工具的設計原則借鑒了以下幾位市場傳奇人物的核心思想：
    - L72-L91 `以傳奇操盤手的眼界設計` — 本工具的設計原則借鑒了以下幾位市場傳奇人物的核心思想：
    - L92-L103 `核心設計原則` — ```
  - L104-L138 `第三章：系統架構一覽` — ```
    - L106-L123 `五個模組的關係` — ```
    - L124-L138 `技術架構（給想了解細節的人）` — | 元件 | 技術 | 說明 |
  - L139-L221 `第四章：如何啟動工具` — 新版提供 `srock` 命令列管理工具，用來統一啟動本機 Dashboard、Basic Auth 反向代理與 Tailscale Funnel。第一次使用若系統還找不到 `srock` 指令，請先在專案資料夾執行一次：
    - L141-L166 `建議啟動方式：Srock CLI` — 新版提供 `srock` 命令列管理工具，用來統一啟動本機 Dashboard、Basic Auth 反向代理與 Tailscale Funnel。第一次使用若系統還找不到 `srock` 指令，請先在專案資料夾執行一次：
    - L167-L186 `Basic Auth 帳號管理` — 若要開啟受保護入口，請先建立至少一組帳密：
    - L187-L199 `傳統啟動 Dashboard（仍可使用）` — 打開黑色視窗（搜尋「命令提示字元」或「PowerShell」）
    - L200-L210 `啟動 LINE 自動推播（讓系統每天自動通知你）` — 另開一個黑色視窗，輸入：
    - L211-L221 `自動推播時間表` — | 時間 | 推播內容 |
- L222-L518 `第二部分：技術分析理論基礎` — 技術分析是一種透過**研究過去的價格和成交量**，來預測未來價格走勢的方法。
  - L226-L256 `第五章：什麼是技術分析` — 技術分析是一種透過**研究過去的價格和成交量**，來預測未來價格走勢的方法。
    - L228-L242 `基本定義` — 技術分析是一種透過**研究過去的價格和成交量**，來預測未來價格走勢的方法。
    - L243-L256 `技術分析 vs. 基本分析` — | 面向 | 技術分析 | 基本分析 |
  - L257-L444 `第六章：各指標的學術根據與可靠性` — 以下逐一說明本工具使用的每個指標，包含它的理論來源和實際可靠程度。
    - L263-L289 `指標一：移動平均線（Moving Average，MA）` — **發明背景**
    - L290-L321 `指標二：成交量（Volume）` — **原理**
    - L322-L362 `指標三：MACD（指數平滑移動平均線）` — **發明背景**
    - L363-L397 `指標四：RSI（相對強弱指數）` — **發明背景**
    - L398-L426 `指標五：布林通道（Bollinger Bands）` — **發明背景**
    - L427-L444 `籌碼面加分：法人買超與融資動向` — **法人買超（外資、投信）**
  - L445-L518 `第七章：技術分析的限制：誠實的評估` — **可靠，但只在特定條件下有優勢，而且優勢是機率性的，不是確定性的。**
    - L447-L452 `這套策略可靠嗎？完整的答案` — **可靠，但只在特定條件下有優勢，而且優勢是機率性的，不是確定性的。**
    - L453-L463 `支持技術分析的學術證據` — | 研究 | 發表期刊 | 結論 |
    - L464-L490 `反對技術分析的論點` — **1. 效率市場假說（Efficient Market Hypothesis，EMH）**
    - L491-L505 `正確認識這套工具的能力邊界` — **一個誠實的比喻：**
    - L506-L518 `各指標可靠性總結` — | 指標 | 學術支持 | 實務穩定性 | 最佳使用情境 | 最差使用情境 |
- L519-L1598 `第三部分：五大功能模組詳解` — 每天掃描台股市場（可選 20 / 100 / 全市場檔），依技術分析條件篩選出符合標準的股票，依強度分數排名輸出。
  - L523-L917 `第八章：選股雷達 — 找值得買的股票` — 每天掃描台股市場（可選 20 / 100 / 全市場檔），依技術分析條件篩選出符合標準的股票，依強度分數排名輸出。
    - L525-L528 `功能說明` — 每天掃描台股市場（可選 20 / 100 / 全市場檔），依技術分析條件篩選出符合標準的股票，依強度分數排名輸出。
    - L529-L540 `掃描範圍選擇建議` — | 選項 | 股票數量 | 所需時間 | 建議使用時機 |
    - L541-L566 `掃描模式與歷史回放` — 新版 Sidebar 多了四個實用功能：
    - L567-L603 `v4 規則總覽` — 目前選股雷達使用的是 **v4 領先攻擊版**。核心思想不再是「已經走很久的強勢股」，而是找出：
    - L604-L622 `v3 / v4 策略差異` — 選股雷達可以在 Sidebar 切換 **v4 領先攻擊版**與 **v3 均線突破版**。兩者不是同一套策略：
    - L623-L639 `補充條件與開關` — 這些條件不是 v4 核心必要條件，但會影響排序或前置過濾：
    - L640-L662 `過熱股防護機制（ATR 動態門檻）` — 選股雷達雖然找啟動股，但仍會防止追得太高。新版改用 **ATR 動態門檻**，取代舊版固定的月線乖離率（%）：
    - L663-L674 `分數怎麼看` — | 強度分數 | 解讀 |
    - L675-L689 `結果欄位說明` — | 欄位 | 意思 |
    - L690-L713 `產業族群分析（第二個頁籤）` — 除了個股排行榜，選股雷達還提供「產業族群分析」功能，幫你看懂資金流向。
    - L714-L727 `流動性前置過濾（掃描前先縮小範圍）` — 在開始掃描前，可以先用流動性條件過濾股票宇宙，聚焦真正值得關注的標的：
    - L728-L754 `產業輪動過濾（只掃最強族群）` — 在流動性過濾之上，還可以再加一層「產業輪動過濾」：
    - L755-L770 `掃描歷史紀錄（第四個頁籤）` — 每次掃描完成後，系統會自動將結果儲存到本機資料庫。在「歷史紀錄」頁籤中可以：
    - L771-L790 `篩選漏斗（🔬 第二個頁籤）` — 掃描完成後，「篩選漏斗」頁籤會顯示每個條件在篩選過程中排除了多少股票，讓你清楚看到條件的嚴格程度。
    - L791-L807 `基本面過濾（財報品質篩選）` — 在 Sidebar 的「基本面過濾」區塊，可以選擇啟用財報條件，在技術篩選之前先排除虧損股與財務地雷。
    - L808-L852 `族群集體突破偵測（🔥 新功能）` — 在 Sidebar「族群集體突破偵測」區塊可啟用兩個進階的族群動量指標，掃描後在「產業族群」頁籤的最底部顯示結果。
    - L853-L893 `籌碼面條件：法人買超（可自訂模式）` — 法人條件現在支援兩種模式，在 Sidebar 的「納入法人買賣超」區塊操作：
    - L894-L917 `正確使用選股雷達的步驟` — ```
  - L918-L1112 `第九章：持股監控 — 管理手上的股票` — 輸入你持有的股票，系統自動計算損益狀態，並在出現 5 種危險訊號時即時發出警示，可推播到 LINE。
    - L920-L923 `功能說明` — 輸入你持有的股票，系統自動計算損益狀態，並在出現 5 種危險訊號時即時發出警示，可推播到 LINE。
    - L924-L941 `如何新增持股` — 進入「管理持股」頁籤，填入以下資料：
    - L942-L961 `持股圖表與布林帶寬` — 持股監控的個股圖表現在會同時畫出：
    - L962-L977 `持股資料可直接在網頁編輯` — 在「管理持股」頁籤中，現在可以直接在表格中批次修改現有資料：
    - L978-L1017 `持股資料匯出 / 匯入` — 「管理持股」頁籤新增了 CSV 匯出與匯入功能。
    - L1018-L1038 `停損價如何設定？` — 這是最重要的欄位。以下是幾種常見的設定方法：
    - L1039-L1100 `五種警示規則完整說明` — **危險一：跌破停損價**
    - L1101-L1112 `持股統計指標說明` — | 指標 | 計算公式 | 說明 |
  - L1113-L1440 `第十章：風險控制 — 每次應該買幾張` — 「很多投資人失敗，不是因為選錯股，而是押注太大。一筆重押的虧損，足以把好幾筆獲利全部抵銷。」
    - L1115-L1128 `為什麼需要科學計算部位大小` — 「很多投資人失敗，不是因為選錯股，而是押注太大。一筆重押的虧損，足以把好幾筆獲利全部抵銷。」
    - L1129-L1171 `計算方法一：固定風險法（日常使用首選）` — **核心原則：每筆交易，最多虧損帳戶總資金的 2%。**
    - L1172-L1215 `計算方法二：Kelly Criterion（有歷史記錄後使用）` — Kelly 公式由數學家 John Larry Kelly Jr. 於 1956 年發表，最初用於電話訊號的傳輸，後來被賭博界和投資界廣泛採用。
    - L1216-L1283 `計算方法三：ATR 正金字塔加碼計畫（趨勢持續時使用）` — 很多人加碼的方式是錯的：股價一跌就想攤平，股價一漲又一次重壓。這樣做會讓風險越來越失控。
    - L1284-L1320 `報酬風險比（R:R）— 每次進場前的必算指標` — **公式：**
    - L1321-L1374 `方法三：ATR 移動停利（持倉中使用）` — **從「固定停利」進化到「動態停利」**
    - L1375-L1387 `帳戶整體曝險監控` — 除了單筆交易的風險，系統也監控你整個帳戶的健康狀況：
    - L1388-L1440 `產業集中度曝險（進階風控）` — **為什麼單看「每筆 2% 風險」還不夠？**
  - L1441-L1504 `第十一章：市場環境 — 積極還是保守` — 個股再好，在空頭環境下也容易被大盤拖累。研究顯示，在多頭環境中，約 70% 的股票會跟著上漲；在空頭環境中，約 70% 的股票會跟著下跌。
    - L1443-L1448 `為什麼要先看大盤` — 個股再好，在空頭環境下也容易被大盤拖累。研究顯示，在多頭環境中，約 70% 的股票會跟著上漲；在空頭環境中，約 70% 的股票會跟著下跌。
    - L1449-L1456 `三個判斷訊號` — | 訊號 | 判斷條件 | 代表意義 |
    - L1457-L1498 `四種環境與對應策略` — **🟢 多頭（3 個訊號都滿足）**
    - L1499-L1504 `一個重要提醒` — 市場環境判斷是大方向的參考，不是 100% 精準的預測。偶爾會出現大盤空頭但個股強勢的情況，或大盤多頭但個股弱勢的情況。環境判斷是機率的考量，不是鐵律。
  - L1505-L1598 `第十二章：交易日誌 — 記錄與學習` — 傳奇操盤手 Paul Tudor Jones 說：「不分析自己的交易，等於在黑暗中射箭。」
    - L1507-L1515 `為什麼記錄比選股更重要` — 傳奇操盤手 Paul Tudor Jones 說：「不分析自己的交易，等於在黑暗中射箭。」
    - L1516-L1528 `每筆交易需要記錄的內容` — | 欄位 | 必填 | 說明 |
    - L1529-L1539 `買進交易會自動同步到持股監控` — 新增交易日誌時，如果動作是 `BUY`，系統會自動把這筆買進同步到「持股監控」：
    - L1540-L1553 `情緒標記的選項與意義` — | 情緒 | 描述 | 常見的錯誤 |
    - L1554-L1598 `三個核心績效指標` — **指標一：勝率**
- L1599-L1963 `第四部分：日常使用指南` — | 時間 | 推播內容 | 觸發條件 |
  - L1603-L1677 `第十三章：LINE 自動推播設定` — | 時間 | 推播內容 | 觸發條件 |
    - L1605-L1613 `推播種類與時間` — | 時間 | 推播內容 | 觸發條件 |
    - L1614-L1643 `推播格式範例` — **選股雷達推播：**
    - L1644-L1647 `手動觸發推播` — 如果不想等自動時間，可以在「市場環境」頁面的「排程控制」頁籤，隨時手動觸發任何推播任務。
    - L1648-L1663 `訂閱者管理（新版）` — 現在推播不再只綁定單一 `LINE_USER_ID`。系統改成：
    - L1664-L1677 `Webhook 自動訂閱` — 如果你有啟動 `webhook.py` 並在 LINE Developers Console 設定 Webhook URL，使用者可以透過 Bot 自動加入或暫停群播：
  - L1678-L1734 `第十四章：每日標準操作流程` — ```
    - L1680-L1694 `開盤前（早上 9:00 前）` — ```
    - L1695-L1702 `盤中（上午 9:00 — 下午 1:30）` — ```
    - L1703-L1720 `收盤後（下午 2:30 — 4:00）` — ```
    - L1721-L1734 `每週五收盤後（特別流程）` — ```
  - L1735-L1799 `第十五章：五大交易紀律` — 這五條紀律是長期獲利的根基，不是選項，是必須遵守的規則。
    - L1741-L1753 `紀律一：嚴格執行停損` — **規則：** 進場前設好停損，到點必須執行，不能找任何理由拖延。
    - L1754-L1761 `紀律二：先看大盤再選股` — **規則：** 開盤前必須先確認市場環境，空頭或偏空環境下不主動建立新倉位。
    - L1762-L1771 `紀律三：部位大小要計算，不要憑感覺` — **規則：** 每次進場前，必須用「風險控制」頁面計算建議張數，不能因為「這次很有把握」就增加張數。
    - L1772-L1786 `紀律四：虧損時絕對不加碼（不攤平）` — **規則：** 買進的股票跌了，絕對不能再買更多來「降低成本」。
    - L1787-L1799 `紀律五：不懂的股票不買` — **規則：** 對於買進的每一檔股票，你需要能夠清楚說出：「我買它的理由是____，我的停損在____，我的目標是____。」
  - L1800-L1853 `第十六章：工具限制與風險提示` — **1. 資料非即時**
    - L1802-L1817 `技術面的限制` — **1. 資料非即時**
    - L1818-L1841 `市場面的風險` — **1. 黑天鵝事件**
    - L1842-L1853 `心理風險（最被低估的風險）` — **連續虧損的影響**
  - L1854-L1963 `第十九章：個股分析 — 逐項檢視單一股票條件` — 在選股雷達找到候選標的後，進入「7 - 個股分析」可以針對**單一股票**深度確認 v3 / v4 技術條件是否真的達標，以及查看完整的圖表與數值細節。
    - L1856-L1861 `功能說明` — 在選股雷達找到候選標的後，進入「7 - 個股分析」可以針對**單一股票**深度確認 v3 / v4 技術條件是否真的達標，以及查看完整的圖表與數值細節。
    - L1862-L1876 `查詢方式` — 在左側側邊欄輸入股票代號（例如 2330），選擇分析基準日後按「🔎 開始分析」。
    - L1877-L1914 `條件評分卡` — 頁面左側會依所選策略版本顯示逐項評分。v4 分為「必要條件」與「加分條件」兩組；v3 則用均線突破、量能、MACD/RSI、布林位置與主力連買來確認。
    - L1915-L1935 `圖表區` — 頁面右側顯示日K線圖，包含：
    - L1936-L1945 `技術指標數值彙整` — 在圖表下方提供一張數值摘要表，列出所有計算中間值：
    - L1946-L1954 `歷史回溯模式` — 在左側選擇**過去的日期**，系統會：
    - L1955-L1963 `注意事項` — 個股分析讀取**本機快取**，若快取無資料會提示去「資料管理」頁面更新
- L1964-L1980 `附錄：未來功能備忘` — 目前 Streamlit App 可以在同一個 Python 程序內啟動背景預抓取工作器，對本機使用最方便，也不需要額外管理服務。
    - L1966-L1980 `Worker 獨立服務化（暫列未來考量）` — 目前 Streamlit App 可以在同一個 Python 程序內啟動背景預抓取工作器，對本機使用最方便，也不需要額外管理服務。
- L1981-L2028 `附錄：常見問題 Q&A` — **Q：選股雷達選出來的股票可以直接買嗎？**
- L2029-L2451 `附錄：名詞解釋` — | 名詞 | 解釋 |
  - L2073-L2264 `第十七章：資料管理 — 快取與背景抓取` — FinMind 免費帳號每小時只有 600 次 API 請求。全市場約 950 檔普通股，每次掃描若都從網路抓取，很快就會超出限制。
    - L2075-L2080 `為什麼需要本機快取？` — FinMind 免費帳號每小時只有 600 次 API 請求。全市場約 950 檔普通股，每次掃描若都從網路抓取，很快就會超出限制。
    - L2081-L2092 `背景預抓取工作器` — App 啟動後，系統會自動在背景開始下載全市場的歷史價格資料。你不需要做任何事，它會靜靜地跑在背景。
    - L2093-L2100 `休市模式` — 連假或停市期間，法人資料不會更新。開啟「🏖️ 休市模式」可讓法人快取永不過期，避免反覆呼叫 API 浪費配額。
    - L2101-L2113 `交易日判斷與參考日期` — 資料管理頁現在會先判斷「今天是否應該有交易資料」，再決定狀態文字與進度基準：
    - L2114-L2136 `資料管理頁（6 - 資料管理）功能說明` — | 功能 | 說明 |
    - L2137-L2150 `快取品質診斷怎麼看` — 「🔍 快取品質診斷」會把本機資料分成 5 類，幫你快速找出為什麼某些股票掃不到、回測跑不動、或資料明顯不完整：
    - L2151-L2172 `市場指數資料（TAIEX）為什麼重要` — 回測模組現在會使用 **TAIEX（台灣加權股價指數）** 當作市場基準。這筆資料主要用在：
    - L2173-L2187 `每日自動更新流程` — 每個交易日，背景工作器會自動依序完成以下四個階段：
    - L2188-L2202 `工作器狀態儀表板說明` — 資料管理頁的狀態卡會顯示目前所在的工作階段，讓你一眼看出系統在做什麼：
    - L2203-L2213 `為什麼法人/融資進度晚於 OHLCV？` — 法人籌碼與融資融券是「Supplementary 補充階段」，有兩個前提條件才會啟動：
    - L2214-L2223 `背景工作器的優先順序` — ```
    - L2224-L2233 `失敗股票與自動略過` — 若某檔股票長期抓不到新資料，系統會自動區分：
    - L2234-L2243 `首次安裝建議流程` — 啟動 App（建議 `srock up --profile local`；傳統方式也可用 `streamlit run app.py`）
    - L2244-L2264 `Yahoo Finance 補充機制` — 系統有兩種使用 Yahoo Finance 的情境，機制不同：
  - L2265-L2451 `第十八章：回測模組 — 驗證策略歷史績效` — 回測模組讓你用歷史資料**模擬**選股策略的過去表現，驗證策略邏輯是否有效，避免用未經驗證的條件進行實際交易。
    - L2267-L2272 `功能說明` — 回測模組讓你用歷史資料**模擬**選股策略的過去表現，驗證策略邏輯是否有效，避免用未經驗證的條件進行實際交易。
    - L2273-L2279 `開始前：確認資料就緒` — 回測模組**不提供內建下載按鈕**，所有歷史資料需透過 **6 - 資料管理** 頁面補充：
    - L2280-L2292 `兩個步驟` — **步驟一：設定參數並執行**（⚙️ 設定回測參數 頁籤）
    - L2293-L2305 `資金管理參數` — 最近版本的回測不再只是「每次看到訊號就固定買 1 檔」。系統改成更接近真實交易的資金管理模型，會根據帳戶大小、剩餘現金與單筆風險限制來決定是否進場與買多少股。
    - L2306-L2323 `系統如何決定買幾股` — 回測引擎會先估算這筆交易的停損距離，再用「單筆風險預算」反推出可買股數。簡化理解如下：
    - L2324-L2346 `報告新增了哪些欄位` — 新版績效報告改成以「帳戶淨值」而不是單純百分比累乘來計算，因此你會看到更多接近實際資金管理的欄位：
    - L2347-L2396 `出場規則：移動式停損/停利` — 回測引擎使用**移動式**出場規則，比傳統固定停損更能讓利潤奔跑：
    - L2397-L2408 `績效指標解讀` — | 指標 | 健康範圍 | 說明 |
    - L2409-L2430 `AI 深度解讀` — 回測結果頁籤底部提供兩種報告輸出方式：
    - L2431-L2451 `回測的常見誤解` — **誤解一：「回測績效 80%，未來也會有 80%」**
