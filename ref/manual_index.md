# 使用說明手冊索引

- 文件: `台股交易輔助工具 — 完整使用說明手冊`
- 路徑: `E:\workspace\srock tool\使用說明手冊.md`
- 版本: `2.4`
- 最後更新: `2026-04-20`
- 總行數: `2682`
- 章節數: `21`

## 使用方式

- 先讀這份 index，確認要看的章節標題與行號。
- 只擷取需要的章節，不要整份手冊一起讀。
- 更新手冊後重新執行 scripts/build_manual_index.py。

## 章節索引

- L1-L9 `台股交易輔助工具 — 完整使用說明手冊` — **版本：** 2.4　　**最後更新：** 2026-04-20
- L10-L47 `目錄` — **第一部分：認識這套工具**
- L48-L235 `第一部分：認識這套工具` — **這套工具是「輔助決策系統」，不是自動下單機器人。**
  - L52-L71 `第一章：這套工具能幫你做什麼` — **這套工具是「輔助決策系統」，不是自動下單機器人。**
    - L54-L59 `工具定位` — **這套工具是「輔助決策系統」，不是自動下單機器人。**
    - L60-L71 `五大核心問題，工具給你答案` — | 你的問題 | 工具的回答 | 對應功能 |
  - L72-L105 `第二章：設計哲學與核心理念` — 本工具的設計原則借鑒了以下幾位市場傳奇人物的核心思想：
    - L74-L93 `以傳奇操盤手的眼界設計` — 本工具的設計原則借鑒了以下幾位市場傳奇人物的核心思想：
    - L94-L105 `核心設計原則` — ```
  - L106-L140 `第三章：系統架構一覽` — ```
    - L108-L125 `五個模組的關係` — ```
    - L126-L140 `技術架構（給想了解細節的人）` — | 元件 | 技術 | 說明 |
  - L141-L235 `第四章：如何啟動工具` — 新版提供 `srock` 命令列管理工具，用來統一啟動本機 Dashboard、Basic Auth 反向代理與 Tailscale Funnel。第一次使用若系統還找不到 `srock` 指令，請先在專案資料夾執行一次：
    - L143-L168 `建議啟動方式：Srock CLI` — 新版提供 `srock` 命令列管理工具，用來統一啟動本機 Dashboard、Basic Auth 反向代理與 Tailscale Funnel。第一次使用若系統還找不到 `srock` 指令，請先在專案資料夾執行一次：
    - L169-L178 `服務啟動完成通知` — 使用 `srock up` 或 `srock restart` 啟動服務時，系統會等 Streamlit、Auth Proxy 與 Cloudflare Tunnel 依照 profile 啟動完成後，推播一則 LINE 訊息給所有啟用中的
    - L179-L198 `Basic Auth 帳號管理` — 若要開啟受保護入口，請先建立至少一組帳密：
    - L199-L211 `傳統啟動 Dashboard（仍可使用）` — 打開黑色視窗（搜尋「命令提示字元」或「PowerShell」）
    - L212-L223 `啟動 LINE 自動推播（盤後任務）` — 另開一個黑色視窗，輸入：
    - L224-L235 `自動推播時間表` — | 時間 | 推播內容 |
- L236-L532 `第二部分：技術分析理論基礎` — 技術分析是一種透過**研究過去的價格和成交量**，來預測未來價格走勢的方法。
  - L240-L270 `第五章：什麼是技術分析` — 技術分析是一種透過**研究過去的價格和成交量**，來預測未來價格走勢的方法。
    - L242-L256 `基本定義` — 技術分析是一種透過**研究過去的價格和成交量**，來預測未來價格走勢的方法。
    - L257-L270 `技術分析 vs. 基本分析` — | 面向 | 技術分析 | 基本分析 |
  - L271-L458 `第六章：各指標的學術根據與可靠性` — 以下逐一說明本工具使用的每個指標，包含它的理論來源和實際可靠程度。
    - L277-L303 `指標一：移動平均線（Moving Average，MA）` — **發明背景**
    - L304-L335 `指標二：成交量（Volume）` — **原理**
    - L336-L376 `指標三：MACD（指數平滑移動平均線）` — **發明背景**
    - L377-L411 `指標四：RSI（相對強弱指數）` — **發明背景**
    - L412-L440 `指標五：布林通道（Bollinger Bands）` — **發明背景**
    - L441-L458 `籌碼面加分：法人買超與融資動向` — **法人買超（外資、投信）**
  - L459-L532 `第七章：技術分析的限制：誠實的評估` — **可靠，但只在特定條件下有優勢，而且優勢是機率性的，不是確定性的。**
    - L461-L466 `這套策略可靠嗎？完整的答案` — **可靠，但只在特定條件下有優勢，而且優勢是機率性的，不是確定性的。**
    - L467-L477 `支持技術分析的學術證據` — | 研究 | 發表期刊 | 結論 |
    - L478-L504 `反對技術分析的論點` — **1. 效率市場假說（Efficient Market Hypothesis，EMH）**
    - L505-L519 `正確認識這套工具的能力邊界` — **一個誠實的比喻：**
    - L520-L532 `各指標可靠性總結` — | 指標 | 學術支持 | 實務穩定性 | 最佳使用情境 | 最差使用情境 |
- L533-L1646 `第三部分：五大功能模組詳解` — 每天掃描台股市場（可選 20 / 100 / 全市場檔），依技術分析條件篩選出符合標準的股票，依強度分數排名輸出。
  - L537-L943 `第八章：選股雷達 — 找值得買的股票` — 每天掃描台股市場（可選 20 / 100 / 全市場檔），依技術分析條件篩選出符合標準的股票，依強度分數排名輸出。
    - L539-L542 `功能說明` — 每天掃描台股市場（可選 20 / 100 / 全市場檔），依技術分析條件篩選出符合標準的股票，依強度分數排名輸出。
    - L543-L556 `掃描範圍選擇建議` — | 選項 | 股票數量 | 所需時間 | 建議使用時機 |
    - L557-L582 `掃描模式與歷史回放` — 新版 Sidebar 多了四個實用功能：
    - L583-L619 `v4 規則總覽` — 目前選股雷達使用的是 **v4 領先攻擊版**。核心思想不再是「已經走很久的強勢股」，而是找出：
    - L620-L638 `v3 / v4 策略差異` — 選股雷達可以在 Sidebar 切換 **v4 領先攻擊版**與 **v3 均線突破版**。兩者不是同一套策略：
    - L639-L655 `補充條件與開關` — 這些條件不是 v4 核心必要條件，但會影響排序或前置過濾：
    - L656-L678 `過熱股防護機制（ATR 動態門檻）` — 選股雷達雖然找啟動股，但仍會防止追得太高。新版改用 **ATR 動態門檻**，取代舊版固定的月線乖離率（%）：
    - L679-L690 `分數怎麼看` — | 強度分數 | 解讀 |
    - L691-L705 `結果欄位說明` — | 欄位 | 意思 |
    - L706-L729 `產業族群分析（第二個頁籤）` — 除了個股排行榜，選股雷達還提供「產業族群分析」功能，幫你看懂資金流向。
    - L730-L743 `流動性前置過濾（掃描前先縮小範圍）` — 在開始掃描前，可以先用流動性條件過濾股票宇宙，聚焦真正值得關注的標的：
    - L744-L770 `產業輪動過濾（只掃最強族群）` — 在流動性過濾之上，還可以再加一層「產業輪動過濾」：
    - L771-L794 `掃描歷史紀錄（第四個頁籤）` — 每次掃描完成後，系統會自動將結果儲存到本機資料庫。在「歷史紀錄」頁籤中可以：
    - L795-L814 `篩選漏斗（🔬 第二個頁籤）` — 掃描完成後，「篩選漏斗」頁籤會顯示每個條件在篩選過程中排除了多少股票，讓你清楚看到條件的嚴格程度。
    - L815-L831 `基本面過濾（財報品質篩選）` — 在 Sidebar 的「基本面過濾」區塊，可以選擇啟用財報條件，在技術篩選之前先排除虧損股與財務地雷。
    - L832-L876 `族群集體突破偵測（🔥 新功能）` — 在 Sidebar「族群集體突破偵測」區塊可啟用兩個進階的族群動量指標，掃描後在「產業族群」頁籤的最底部顯示結果。
    - L877-L919 `籌碼面條件：法人買超（可自訂模式）` — 三大法人條件現在支援兩種模式，在 Sidebar 的「納入三大法人買賣超」區塊操作：
    - L920-L943 `正確使用選股雷達的步驟` — ```
  - L944-L1158 `第九章：持股監控 — 管理手上的股票` — 輸入你持有的股票，系統自動計算損益狀態，並在出現 5 種危險訊號時即時發出警示，可推播到 LINE。
    - L946-L949 `功能說明` — 輸入你持有的股票，系統自動計算損益狀態，並在出現 5 種危險訊號時即時發出警示，可推播到 LINE。
    - L950-L967 `如何新增持股` — 進入「管理持股」頁籤，填入以下資料：
    - L968-L987 `持股圖表與布林帶寬` — 持股監控的個股圖表現在會同時畫出：
    - L988-L1003 `持股資料可直接在網頁編輯` — 在「管理持股」頁籤中，現在可以直接在表格中批次修改現有資料：
    - L1004-L1043 `持股資料匯出 / 匯入` — 「管理持股」頁籤新增了 CSV 匯出與匯入功能。
    - L1044-L1064 `停損價如何設定？` — 這是最重要的欄位。以下是幾種常見的設定方法：
    - L1065-L1126 `五種警示規則完整說明` — **危險一：跌破停損價**
    - L1127-L1146 `Premium 風險警示（Sponsor 試用）` — 若啟用 FinMind Sponsor / Premium 功能，持股監控會在既有停損、停利、均線、回撤與布林警示之外，額外讀取本機快取中的 Premium 風險訊號。這些警示不會批次呼叫 Premium API，只使用已經存在的 cach
    - L1147-L1158 `持股統計指標說明` — | 指標 | 計算公式 | 說明 |
  - L1159-L1486 `第十章：風險控制 — 每次應該買幾張` — 「很多投資人失敗，不是因為選錯股，而是押注太大。一筆重押的虧損，足以把好幾筆獲利全部抵銷。」
    - L1161-L1174 `為什麼需要科學計算部位大小` — 「很多投資人失敗，不是因為選錯股，而是押注太大。一筆重押的虧損，足以把好幾筆獲利全部抵銷。」
    - L1175-L1217 `計算方法一：固定風險法（日常使用首選）` — **核心原則：每筆交易，最多虧損帳戶總資金的 2%。**
    - L1218-L1261 `計算方法二：Kelly Criterion（有歷史記錄後使用）` — Kelly 公式由數學家 John Larry Kelly Jr. 於 1956 年發表，最初用於電話訊號的傳輸，後來被賭博界和投資界廣泛採用。
    - L1262-L1329 `計算方法三：ATR 正金字塔加碼計畫（趨勢持續時使用）` — 很多人加碼的方式是錯的：股價一跌就想攤平，股價一漲又一次重壓。這樣做會讓風險越來越失控。
    - L1330-L1366 `報酬風險比（R:R）— 每次進場前的必算指標` — **公式：**
    - L1367-L1420 `方法三：ATR 移動停利（持倉中使用）` — **從「固定停利」進化到「動態停利」**
    - L1421-L1433 `帳戶整體曝險監控` — 除了單筆交易的風險，系統也監控你整個帳戶的健康狀況：
    - L1434-L1486 `產業集中度曝險（進階風控）` — **為什麼單看「每筆 2% 風險」還不夠？**
  - L1487-L1550 `第十一章：市場環境 — 積極還是保守` — 個股再好，在空頭環境下也容易被大盤拖累。研究顯示，在多頭環境中，約 70% 的股票會跟著上漲；在空頭環境中，約 70% 的股票會跟著下跌。
    - L1489-L1494 `為什麼要先看大盤` — 個股再好，在空頭環境下也容易被大盤拖累。研究顯示，在多頭環境中，約 70% 的股票會跟著上漲；在空頭環境中，約 70% 的股票會跟著下跌。
    - L1495-L1502 `三個判斷訊號` — | 訊號 | 判斷條件 | 代表意義 |
    - L1503-L1544 `四種環境與對應策略` — **🟢 多頭（3 個訊號都滿足）**
    - L1545-L1550 `一個重要提醒` — 市場環境判斷是大方向的參考，不是 100% 精準的預測。偶爾會出現大盤空頭但個股強勢的情況，或大盤多頭但個股弱勢的情況。環境判斷是機率的考量，不是鐵律。
  - L1551-L1646 `第十二章：交易日誌 — 記錄與學習` — 傳奇操盤手 Paul Tudor Jones 說：「不分析自己的交易，等於在黑暗中射箭。」
    - L1553-L1561 `為什麼記錄比選股更重要` — 傳奇操盤手 Paul Tudor Jones 說：「不分析自己的交易，等於在黑暗中射箭。」
    - L1562-L1574 `每筆交易需要記錄的內容` — | 欄位 | 必填 | 說明 |
    - L1575-L1587 `買賣交易會自動同步到持股監控` — 新增交易日誌時，系統會自動同步「持股監控」：
    - L1588-L1601 `情緒標記的選項與意義` — | 情緒 | 描述 | 常見的錯誤 |
    - L1602-L1646 `三個核心績效指標` — **指標一：勝率**
- L1647-L2148 `第四部分：日常使用指南` — | 時間 | 推播內容 | 觸發條件 |
  - L1651-L1725 `第十三章：LINE 自動推播設定` — | 時間 | 推播內容 | 觸發條件 |
    - L1653-L1661 `推播種類與時間` — | 時間 | 推播內容 | 觸發條件 |
    - L1662-L1691 `推播格式範例` — **選股雷達推播：**
    - L1692-L1695 `手動觸發推播` — 如果不想等自動時間，可以在「市場環境」頁面的「排程控制」頁籤，隨時手動觸發任何推播任務。
    - L1696-L1711 `訂閱者管理（新版）` — 現在推播不再只綁定單一 `LINE_USER_ID`。系統改成：
    - L1712-L1725 `Webhook 自動訂閱` — 如果你有啟動 `webhook.py` 並在 LINE Developers Console 設定 Webhook URL，使用者可以透過 Bot 自動加入或暫停群播：
  - L1726-L1782 `第十四章：每日標準操作流程` — ```
    - L1728-L1742 `開盤前（早上 9:00 前）` — ```
    - L1743-L1750 `盤中（上午 9:00 — 下午 1:30）` — ```
    - L1751-L1768 `收盤後（下午 2:30 — 4:00）` — ```
    - L1769-L1782 `每週五收盤後（特別流程）` — ```
  - L1783-L1847 `第十五章：五大交易紀律` — 這五條紀律是長期獲利的根基，不是選項，是必須遵守的規則。
    - L1789-L1801 `紀律一：嚴格執行停損` — **規則：** 進場前設好停損，到點必須執行，不能找任何理由拖延。
    - L1802-L1809 `紀律二：先看大盤再選股` — **規則：** 開盤前必須先確認市場環境，空頭或偏空環境下不主動建立新倉位。
    - L1810-L1819 `紀律三：部位大小要計算，不要憑感覺` — **規則：** 每次進場前，必須用「風險控制」頁面計算建議張數，不能因為「這次很有把握」就增加張數。
    - L1820-L1834 `紀律四：虧損時絕對不加碼（不攤平）` — **規則：** 買進的股票跌了，絕對不能再買更多來「降低成本」。
    - L1835-L1847 `紀律五：不懂的股票不買` — **規則：** 對於買進的每一檔股票，你需要能夠清楚說出：「我買它的理由是____，我的停損在____，我的目標是____。」
  - L1848-L1901 `第十六章：工具限制與風險提示` — **1. 大多數策略資料不是即時**
    - L1850-L1865 `技術面的限制` — **1. 大多數策略資料不是即時**
    - L1866-L1889 `市場面的風險` — **1. 黑天鵝事件**
    - L1890-L1901 `心理風險（最被低估的風險）` — **連續虧損的影響**
  - L1902-L2030 `第十九章：個股分析 — 逐項檢視單一股票條件` — 在選股雷達找到候選標的後，進入「7 - 個股分析」可以針對**單一股票**深度確認 v3 / v4 技術條件是否真的達標，以及查看完整的圖表與數值細節。
    - L1904-L1909 `功能說明` — 在選股雷達找到候選標的後，進入「7 - 個股分析」可以針對**單一股票**深度確認 v3 / v4 技術條件是否真的達標，以及查看完整的圖表與數值細節。
    - L1910-L1924 `查詢方式` — 在左側側邊欄輸入股票代號（例如 2330），選擇分析基準日後按「🔎 開始分析」。
    - L1925-L1930 `盤中即時資訊（Sponsor）` — 今日模式下，個股分析會顯示「盤中即時資訊」區塊。此區塊使用 FinMind Sponsor 的 `taiwan_stock_tick_snapshot` 即時快照，顯示最新成交價、漲跌、買賣價、累積量、開高低與資料時間。
    - L1931-L1968 `條件評分卡` — 頁面左側會依所選策略版本顯示逐項評分。v4 分為「必要條件」與「加分條件」兩組；v3 則用均線突破、量能、MACD/RSI、布林位置與主力連買來確認。
    - L1969-L1989 `圖表區` — 頁面右側顯示日K線圖，包含：
    - L1990-L1999 `技術指標數值彙整` — 在圖表下方提供一張數值摘要表，列出所有計算中間值：
    - L2000-L2008 `歷史回溯模式` — 在左側選擇**過去的日期**，系統會：
    - L2009-L2020 `Premium 摘要區（Sponsor 試用）` — 若啟用 FinMind Sponsor / Premium 功能，個股分析頁會在技術條件之外顯示 Premium 摘要。這個區塊以本機快取與頁面已載入資料為主，不會為了摘要額外批次呼叫 Premium API。
    - L2021-L2030 `注意事項` — 個股分析的日K與歷史模式讀取**本機快取**，若快取無資料會提示去「資料管理」頁面更新
  - L2031-L2114 `第二十章：FinMind Sponsor Premium 試用功能` — Sponsor Premium 功能是這套工具的「增強層」，不是核心策略本身。v3 / v4 的技術條件、掃描流程、持股監控與風控原則都可以在 Free 模式下運作。Premium 資料只用來補強三件事：
    - L2033-L2040 `功能定位` — Sponsor Premium 功能是這套工具的「增強層」，不是核心策略本身。v3 / v4 的技術條件、掃描流程、持股監控與風控原則都可以在 Free 模式下運作。Premium 資料只用來補強三件事：
    - L2041-L2049 `降級原則` — 系統採用「可關閉、可降級、可觀察」的設計：
    - L2050-L2058 `在各頁面看到什麼` — | 頁面 | Premium 相關內容 | 是否會額外呼叫 Premium API |
    - L2059-L2078 `目前資料種類與使用狀態` — | 資料類別 | FinMind dataset | 權限 | 本機快取 / 狀態 | 目前用在哪裡 |
    - L2079-L2085 `Known Issues / 後續確認` — `TaiwanStockShareholdingTransfer`（申報轉讓）目前列入管線與顯示，但實際資料來源仍需確認。若個別股票新聞有申報轉讓、FinMind 回傳沒有資料，先不要把它視為程式漏抓。
    - L2086-L2101 `分數欄位怎麼看` — 選股雷達現在保留原本的 `score` 相容性，同時拆出更清楚的欄位：
    - L2102-L2114 `試用期評估怎麼用` — 進入「選股雷達 → 歷史紀錄」，上方會看到 Premium 試用期評估。建議用法：
  - L2115-L2148 `第二十一章：問題回報 — 回報問題與功能建議` — 「問題回報」頁面用來收集使用者遇到的錯誤、操作疑問與功能建議。送出後，系統會把內容存成 Markdown 檔，放在專案根目錄的 `user_reports/` 資料夾，方便後續集中整理與處理。
    - L2117-L2120 `功能說明` — 「問題回報」頁面用來收集使用者遇到的錯誤、操作疑問與功能建議。送出後，系統會把內容存成 Markdown 檔，放在專案根目錄的 `user_reports/` 資料夾，方便後續集中整理與處理。
    - L2121-L2129 `問題回報` — 選擇「問題回報」後，需要填寫：
    - L2130-L2136 `功能新增` — 選擇「功能新增」後，需要填寫：
    - L2137-L2148 `檔案保存位置` — 每次送出會建立一個獨立 Markdown 檔，檔名包含時間、回報類型與頁面名稱，例如：
- L2149-L2165 `附錄：未來功能備忘` — 目前 Streamlit App 可以在同一個 Python 程序內啟動背景預抓取工作器，對本機使用最方便，也不需要額外管理服務。
    - L2151-L2165 `Worker 獨立服務化（暫列未來考量）` — 目前 Streamlit App 可以在同一個 Python 程序內啟動背景預抓取工作器，對本機使用最方便，也不需要額外管理服務。
- L2166-L2214 `附錄：常見問題 Q&A` — **Q：選股雷達選出來的股票可以直接買嗎？**
- L2215-L2682 `附錄：名詞解釋` — | 名詞 | 解釋 |
  - L2259-L2495 `第十七章：資料管理 — 快取與背景抓取` — FinMind 免費帳號每小時只有 600 次 API 請求；Sponsor 帳號可用額度較高，目前系統會依 `user_info` 回傳額度調整，Sponsor 盤後/非交易時間可用到每小時 6000 次。全市場約 950 檔普通股，每次
    - L2261-L2266 `為什麼需要本機快取？` — FinMind 免費帳號每小時只有 600 次 API 請求；Sponsor 帳號可用額度較高，目前系統會依 `user_info` 回傳額度調整，Sponsor 盤後/非交易時間可用到每小時 6000 次。全市場約 950 檔普通股，每次
    - L2267-L2278 `背景預抓取工作器` — App 啟動後，系統會自動在背景開始下載全市場的歷史價格資料。你不需要做任何事，它會靜靜地跑在背景。
    - L2279-L2286 `休市模式` — 連假或停市期間，法人資料不會更新。開啟「🏖️ 休市模式」可讓法人快取永不過期，避免反覆呼叫 API 浪費配額。
    - L2287-L2299 `交易日判斷與參考日期` — 資料管理頁現在會先判斷「今天是否應該有交易資料」，再決定狀態文字與進度基準：
    - L2300-L2327 `資料管理頁（6 - 資料管理）功能說明` — 進入「資料管理」頁時，系統會確認內建背景工作器是否正在執行；若工作器尚未啟動，頁面會自動啟動它，不需要使用者再手動按一次「啟動」。這是為了避免服務已上線但快取沒有自動補資料的情況。
    - L2328-L2343 `目前資料庫狀態快照（2026-04-20 08:58）` — 目前正式資料庫是專案根目錄的 `srock.db`，不是 `data/stock_data.db`。`data/stock_data.db` 是 0 bytes 舊路徑，不應用來判斷資料完整度。
    - L2344-L2357 `快取品質診斷怎麼看` — 「🔍 快取品質診斷」會把本機資料分成 5 類，幫你快速找出為什麼某些股票掃不到、回測跑不動、或資料明顯不完整：
    - L2358-L2379 `市場指數資料（TAIEX）為什麼重要` — 回測模組現在會使用 **TAIEX（台灣加權股價指數）** 當作市場基準。這筆資料主要用在：
    - L2380-L2397 `每日自動更新流程` — 每個交易日，背景工作器會自動依序完成以下階段：
    - L2398-L2413 `工作器狀態儀表板說明` — 資料管理頁的狀態卡會顯示目前所在的工作階段，讓你一眼看出系統在做什麼：
    - L2414-L2424 `為什麼法人/融資進度晚於 OHLCV？` — 法人籌碼與融資融券是「Supplementary 補充階段」，有兩個前提條件才會啟動：
    - L2425-L2437 `背景工作器的優先順序` — ```
    - L2438-L2454 `主力券商補完排查紀錄（2026-04-20）` — 2026-04-20 早上排查時發現，主力券商補完前一晚雖然有執行，但補完進度不合理。原因不是 API 額度不足，而是舊版欄位對應造成壞快取：
    - L2455-L2464 `失敗股票與自動略過` — 若某檔股票長期抓不到新資料，系統會自動區分：
    - L2465-L2474 `首次安裝建議流程` — 啟動 App（建議 `srock up --profile local`；傳統方式也可用 `streamlit run app.py`）
    - L2475-L2495 `Yahoo Finance 補充機制` — 系統有兩種使用 Yahoo Finance 的情境，機制不同：
  - L2496-L2682 `第十八章：回測模組 — 驗證策略歷史績效` — 回測模組讓你用歷史資料**模擬**選股策略的過去表現，驗證策略邏輯是否有效，避免用未經驗證的條件進行實際交易。
    - L2498-L2503 `功能說明` — 回測模組讓你用歷史資料**模擬**選股策略的過去表現，驗證策略邏輯是否有效，避免用未經驗證的條件進行實際交易。
    - L2504-L2510 `開始前：確認資料就緒` — 回測模組**不提供內建下載按鈕**，所有歷史資料需透過 **6 - 資料管理** 頁面補充：
    - L2511-L2523 `兩個步驟` — **步驟一：設定參數並執行**（⚙️ 設定回測參數 頁籤）
    - L2524-L2536 `資金管理參數` — 最近版本的回測不再只是「每次看到訊號就固定買 1 檔」。系統改成更接近真實交易的資金管理模型，會根據帳戶大小、剩餘現金與單筆風險限制來決定是否進場與買多少股。
    - L2537-L2554 `系統如何決定買幾股` — 回測引擎會先估算這筆交易的停損距離，再用「單筆風險預算」反推出可買股數。簡化理解如下：
    - L2555-L2577 `報告新增了哪些欄位` — 新版績效報告改成以「帳戶淨值」而不是單純百分比累乘來計算，因此你會看到更多接近實際資金管理的欄位：
    - L2578-L2627 `出場規則：移動式停損/停利` — 回測引擎使用**移動式**出場規則，比傳統固定停損更能讓利潤奔跑：
    - L2628-L2639 `績效指標解讀` — | 指標 | 健康範圍 | 說明 |
    - L2640-L2661 `AI 深度解讀` — 回測結果頁籤底部提供兩種報告輸出方式：
    - L2662-L2682 `回測的常見誤解` — **誤解一：「回測績效 80%，未來也會有 80%」**
