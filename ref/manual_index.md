# 使用說明手冊索引

- 文件: `台股交易輔助工具 — 完整使用說明手冊`
- 路徑: `E:\workspace\srock tool\使用說明手冊.md`
- 版本: `3.0`
- 最後更新: `2026-05-01`
- 總行數: `3155`
- 章節數: `24`

## 使用方式

- 先讀這份 index，確認要看的章節標題與行號。
- 只擷取需要的章節，不要整份手冊一起讀。
- 更新手冊後重新執行 scripts/build_manual_index.py。

## 章節索引

- L1-L9 `台股交易輔助工具 — 完整使用說明手冊` — **版本：** 3.0　　**最後更新：** 2026-05-01
- L10-L50 `目錄` — **第一部分：認識這套工具**
- L51-L239 `第一部分：認識這套工具` — **這套工具是「輔助決策系統」，不是自動下單機器人。**
  - L55-L74 `第一章：這套工具能幫你做什麼` — **這套工具是「輔助決策系統」，不是自動下單機器人。**
    - L57-L62 `工具定位` — **這套工具是「輔助決策系統」，不是自動下單機器人。**
    - L63-L74 `五大核心問題，工具給你答案` — | 你的問題 | 工具的回答 | 對應功能 |
  - L75-L108 `第二章：設計哲學與核心理念` — 本工具的設計原則借鑒了以下幾位市場傳奇人物的核心思想：
    - L77-L96 `以傳奇操盤手的眼界設計` — 本工具的設計原則借鑒了以下幾位市場傳奇人物的核心思想：
    - L97-L108 `核心設計原則` — ```
  - L109-L144 `第三章：系統架構一覽` — ```
    - L111-L128 `五個模組的關係` — ```
    - L129-L144 `技術架構（給想了解細節的人）` — | 元件 | 技術 | 說明 |
  - L145-L239 `第四章：如何啟動工具` — 新版提供 `srock` 命令列管理工具，用來統一啟動本機 Dashboard、Basic Auth 反向代理與 Tailscale Funnel。第一次使用若系統還找不到 `srock` 指令，請先在專案資料夾執行一次：
    - L147-L172 `建議啟動方式：Srock CLI` — 新版提供 `srock` 命令列管理工具，用來統一啟動本機 Dashboard、Basic Auth 反向代理與 Tailscale Funnel。第一次使用若系統還找不到 `srock` 指令，請先在專案資料夾執行一次：
    - L173-L182 `服務啟動完成通知` — 使用 `srock up` 或 `srock restart` 啟動服務時，系統會等 Streamlit、Auth Proxy 與 Cloudflare Tunnel 依照 profile 啟動完成後，推播一則 LINE 訊息給所有啟用中的
    - L183-L202 `Basic Auth 帳號管理` — 若要開啟受保護入口，請先建立至少一組帳密：
    - L203-L215 `傳統啟動 Dashboard（仍可使用）` — 打開黑色視窗（搜尋「命令提示字元」或「PowerShell」）
    - L216-L227 `啟動 LINE 自動推播（盤後任務）` — 另開一個黑色視窗，輸入：
    - L228-L239 `自動推播時間表` — | 時間 | 推播內容 |
- L240-L536 `第二部分：技術分析理論基礎` — 技術分析是一種透過**研究過去的價格和成交量**，來預測未來價格走勢的方法。
  - L244-L274 `第五章：什麼是技術分析` — 技術分析是一種透過**研究過去的價格和成交量**，來預測未來價格走勢的方法。
    - L246-L260 `基本定義` — 技術分析是一種透過**研究過去的價格和成交量**，來預測未來價格走勢的方法。
    - L261-L274 `技術分析 vs. 基本分析` — | 面向 | 技術分析 | 基本分析 |
  - L275-L462 `第六章：各指標的學術根據與可靠性` — 以下逐一說明本工具使用的每個指標，包含它的理論來源和實際可靠程度。
    - L281-L307 `指標一：移動平均線（Moving Average，MA）` — **發明背景**
    - L308-L339 `指標二：成交量（Volume）` — **原理**
    - L340-L380 `指標三：MACD（指數平滑移動平均線）` — **發明背景**
    - L381-L415 `指標四：RSI（相對強弱指數）` — **發明背景**
    - L416-L444 `指標五：布林通道（Bollinger Bands）` — **發明背景**
    - L445-L462 `籌碼面加分：法人買超與融資動向` — **法人買超（外資、投信）**
  - L463-L536 `第七章：技術分析的限制：誠實的評估` — **可靠，但只在特定條件下有優勢，而且優勢是機率性的，不是確定性的。**
    - L465-L470 `這套策略可靠嗎？完整的答案` — **可靠，但只在特定條件下有優勢，而且優勢是機率性的，不是確定性的。**
    - L471-L481 `支持技術分析的學術證據` — | 研究 | 發表期刊 | 結論 |
    - L482-L508 `反對技術分析的論點` — **1. 效率市場假說（Efficient Market Hypothesis，EMH）**
    - L509-L523 `正確認識這套工具的能力邊界` — **一個誠實的比喻：**
    - L524-L536 `各指標可靠性總結` — | 指標 | 學術支持 | 實務穩定性 | 最佳使用情境 | 最差使用情境 |
- L537-L1725 `第三部分：五大功能模組詳解` — 每天掃描台股市場（可選 20 / 100 / 全市場檔），依技術分析條件篩選出符合標準的股票，依強度分數排名輸出。
  - L541-L982 `第八章：選股雷達 — 找值得買的股票` — 每天掃描台股市場（可選 20 / 100 / 全市場檔），依技術分析條件篩選出符合標準的股票，依強度分數排名輸出。
    - L543-L546 `功能說明` — 每天掃描台股市場（可選 20 / 100 / 全市場檔），依技術分析條件篩選出符合標準的股票，依強度分數排名輸出。
    - L547-L560 `掃描範圍選擇建議` — | 選項 | 股票數量 | 所需時間 | 建議使用時機 |
    - L561-L586 `掃描模式與歷史回放` — 新版 Sidebar 多了四個實用功能：
    - L587-L626 `v4 規則總覽` — 目前選股雷達使用的是 **v4 領先攻擊版**。核心思想不再是「已經走很久的強勢股」，而是找出：
    - L627-L645 `v3 / v4 策略差異` — 選股雷達可以在 Sidebar 切換 **v4 領先攻擊版**與 **v3 均線突破版**。兩者不是同一套策略：
    - L646-L662 `補充條件與開關` — 這些條件不是 v4 核心必要條件，但會影響排序或前置過濾：
    - L663-L685 `過熱股防護機制（ATR 動態門檻）` — 選股雷達雖然找啟動股，但仍會防止追得太高。新版改用 **ATR 動態門檻**，取代舊版固定的月線乖離率（%）：
    - L686-L701 `法人新動向篩選（進階選項）` — 在 Sidebar「🔬 進階選項」裡，有三個額外的法人訊號篩選 checkbox：
    - L702-L713 `分數怎麼看` — | 強度分數 | 解讀 |
    - L714-L728 `結果欄位說明` — | 欄位 | 意思 |
    - L729-L752 `產業族群分析（第二個頁籤）` — 除了個股排行榜，選股雷達還提供「產業族群分析」功能，幫你看懂資金流向。
    - L753-L766 `流動性前置過濾（掃描前先縮小範圍）` — 在開始掃描前，可以先用流動性條件過濾股票宇宙，聚焦真正值得關注的標的：
    - L767-L793 `產業輪動過濾（只掃最強族群）` — 在流動性過濾之上，還可以再加一層「產業輪動過濾」：
    - L794-L817 `掃描歷史紀錄（第四個頁籤）` — 每次掃描完成後，系統會自動將結果儲存到本機資料庫。在「歷史紀錄」頁籤中可以：
    - L818-L837 `篩選漏斗（🔬 第二個頁籤）` — 掃描完成後，「篩選漏斗」頁籤會顯示每個條件在篩選過程中排除了多少股票，讓你清楚看到條件的嚴格程度。
    - L838-L854 `基本面過濾（財報品質篩選）` — 在 Sidebar 的「基本面過濾」區塊，可以選擇啟用財報條件，在技術篩選之前先排除虧損股與財務地雷。
    - L855-L899 `族群集體突破偵測（🔥 新功能）` — 在 Sidebar「族群集體突破偵測」區塊可啟用兩個進階的族群動量指標，掃描後在「產業族群」頁籤的最底部顯示結果。
    - L900-L940 `籌碼面條件：法人買超（可自訂模式）` — 三大法人條件現在支援兩種模式，在 Sidebar 的「納入三大法人買賣超」區塊操作：
    - L941-L958 `掃描前法人資料確認 Dialog` — 當你啟用了法人條件（必要條件或加分條件），並且執行**今日掃描**（非歷史回放）時，系統會先確認法人資料的完整程度。若 FinMind 今日尚未充分發布法人資料，頁面會彈出確認對話框。
    - L959-L982 `正確使用選股雷達的步驟` — ```
  - L983-L1237 `第九章：持股監控 — 管理手上的股票` — 輸入你持有的股票，系統自動計算損益狀態，並在出現 5 種危險訊號時即時發出警示，可推播到 LINE。
    - L985-L988 `功能說明` — 輸入你持有的股票，系統自動計算損益狀態，並在出現 5 種危險訊號時即時發出警示，可推播到 LINE。
    - L989-L1006 `如何新增持股` — 進入「管理持股」頁籤，填入以下資料：
    - L1007-L1026 `持股圖表與布林帶寬` — 持股監控的個股圖表現在會同時畫出：
    - L1027-L1042 `持股資料可直接在網頁編輯` — 在「管理持股」頁籤中，現在可以直接在表格中批次修改現有資料：
    - L1043-L1082 `持股資料匯出 / 匯入` — 「管理持股」頁籤新增了 CSV 匯出與匯入功能。
    - L1083-L1103 `停損價如何設定？` — 這是最重要的欄位。以下是幾種常見的設定方法：
    - L1104-L1165 `五種警示規則完整說明` — **危險一：跌破停損價**
    - L1166-L1185 `Premium 風險警示（Sponsor 試用）` — 若啟用 FinMind Sponsor / Premium 功能，持股監控會在既有停損、停利、均線、回撤與布林警示之外，額外讀取本機快取中的 Premium 風險訊號。這些警示不會批次呼叫 Premium API，只使用已經存在的 cach
    - L1186-L1197 `持股統計指標說明` — | 指標 | 計算公式 | 說明 |
    - L1198-L1237 `週報：籌碼面背離偵測` — 「📆 週報」頁籤以每週為單位，比對大戶（>1000張）與散戶（≤50張）的持股比例變化，搭配該週股價漲跌，偵測兩種籌碼異常型態。
  - L1238-L1565 `第十章：風險控制 — 每次應該買幾張` — 「很多投資人失敗，不是因為選錯股，而是押注太大。一筆重押的虧損，足以把好幾筆獲利全部抵銷。」
    - L1240-L1253 `為什麼需要科學計算部位大小` — 「很多投資人失敗，不是因為選錯股，而是押注太大。一筆重押的虧損，足以把好幾筆獲利全部抵銷。」
    - L1254-L1296 `計算方法一：固定風險法（日常使用首選）` — **核心原則：每筆交易，最多虧損帳戶總資金的 2%。**
    - L1297-L1340 `計算方法二：Kelly Criterion（有歷史記錄後使用）` — Kelly 公式由數學家 John Larry Kelly Jr. 於 1956 年發表，最初用於電話訊號的傳輸，後來被賭博界和投資界廣泛採用。
    - L1341-L1408 `計算方法三：ATR 正金字塔加碼計畫（趨勢持續時使用）` — 很多人加碼的方式是錯的：股價一跌就想攤平，股價一漲又一次重壓。這樣做會讓風險越來越失控。
    - L1409-L1445 `報酬風險比（R:R）— 每次進場前的必算指標` — **公式：**
    - L1446-L1499 `方法三：ATR 移動停利（持倉中使用）` — **從「固定停利」進化到「動態停利」**
    - L1500-L1512 `帳戶整體曝險監控` — 除了單筆交易的風險，系統也監控你整個帳戶的健康狀況：
    - L1513-L1565 `產業集中度曝險（進階風控）` — **為什麼單看「每筆 2% 風險」還不夠？**
  - L1566-L1629 `第十一章：市場環境 — 積極還是保守` — 個股再好，在空頭環境下也容易被大盤拖累。研究顯示，在多頭環境中，約 70% 的股票會跟著上漲；在空頭環境中，約 70% 的股票會跟著下跌。
    - L1568-L1573 `為什麼要先看大盤` — 個股再好，在空頭環境下也容易被大盤拖累。研究顯示，在多頭環境中，約 70% 的股票會跟著上漲；在空頭環境中，約 70% 的股票會跟著下跌。
    - L1574-L1581 `三個判斷訊號` — | 訊號 | 判斷條件 | 代表意義 |
    - L1582-L1623 `四種環境與對應策略` — **🟢 多頭（3 個訊號都滿足）**
    - L1624-L1629 `一個重要提醒` — 市場環境判斷是大方向的參考，不是 100% 精準的預測。偶爾會出現大盤空頭但個股強勢的情況，或大盤多頭但個股弱勢的情況。環境判斷是機率的考量，不是鐵律。
  - L1630-L1725 `第十二章：交易日誌 — 記錄與學習` — 傳奇操盤手 Paul Tudor Jones 說：「不分析自己的交易，等於在黑暗中射箭。」
    - L1632-L1640 `為什麼記錄比選股更重要` — 傳奇操盤手 Paul Tudor Jones 說：「不分析自己的交易，等於在黑暗中射箭。」
    - L1641-L1653 `每筆交易需要記錄的內容` — | 欄位 | 必填 | 說明 |
    - L1654-L1666 `買賣交易會自動同步到持股監控` — 新增交易日誌時，系統會自動同步「持股監控」：
    - L1667-L1680 `情緒標記的選項與意義` — | 情緒 | 描述 | 常見的錯誤 |
    - L1681-L1725 `三個核心績效指標` — **指標一：勝率**
- L1726-L2531 `第四部分：日常使用指南` — | 時間 | 推播內容 | 觸發條件 |
  - L1730-L1823 `第十三章：LINE 自動推播設定` — | 時間 | 推播內容 | 觸發條件 |
    - L1732-L1742 `推播種類與時間` — | 時間 | 推播內容 | 觸發條件 |
    - L1743-L1759 `國定假日自動跳過（交易日守門）` — 排程器除了 cron 的「週一至週五」過濾外，**所有與盤勢有關的任務都會在每次觸發時再查一次台股官方交易日曆**。落在週一到週五但休市的國定假日（例如勞動節 5/1、農曆春節、國慶連假等），這些任務會直接 return 不執行，避免推送異
    - L1760-L1789 `推播格式範例` — **選股雷達推播：**
    - L1790-L1793 `手動觸發推播` — 如果不想等自動時間，可以在「市場環境」頁面的「排程控制」頁籤，隨時手動觸發任何推播任務。
    - L1794-L1809 `訂閱者管理（新版）` — 現在推播不再只綁定單一 `LINE_USER_ID`。系統改成：
    - L1810-L1823 `Webhook 自動訂閱` — 如果你有啟動 `webhook.py` 並在 LINE Developers Console 設定 Webhook URL，使用者可以透過 Bot 自動加入或暫停群播：
  - L1824-L1880 `第十四章：每日標準操作流程` — ```
    - L1826-L1840 `開盤前（早上 9:00 前）` — ```
    - L1841-L1848 `盤中（上午 9:00 — 下午 1:30）` — ```
    - L1849-L1866 `收盤後（下午 2:30 — 4:00）` — ```
    - L1867-L1880 `每週五收盤後（特別流程）` — ```
  - L1881-L1945 `第十五章：五大交易紀律` — 這五條紀律是長期獲利的根基，不是選項，是必須遵守的規則。
    - L1887-L1899 `紀律一：嚴格執行停損` — **規則：** 進場前設好停損，到點必須執行，不能找任何理由拖延。
    - L1900-L1907 `紀律二：先看大盤再選股` — **規則：** 開盤前必須先確認市場環境，空頭或偏空環境下不主動建立新倉位。
    - L1908-L1917 `紀律三：部位大小要計算，不要憑感覺` — **規則：** 每次進場前，必須用「風險控制」頁面計算建議張數，不能因為「這次很有把握」就增加張數。
    - L1918-L1932 `紀律四：虧損時絕對不加碼（不攤平）` — **規則：** 買進的股票跌了，絕對不能再買更多來「降低成本」。
    - L1933-L1945 `紀律五：不懂的股票不買` — **規則：** 對於買進的每一檔股票，你需要能夠清楚說出：「我買它的理由是____，我的停損在____，我的目標是____。」
  - L1946-L1999 `第十六章：工具限制與風險提示` — **1. 大多數策略資料不是即時**
    - L1948-L1963 `技術面的限制` — **1. 大多數策略資料不是即時**
    - L1964-L1987 `市場面的風險` — **1. 黑天鵝事件**
    - L1988-L1999 `心理風險（最被低估的風險）` — **連續虧損的影響**
  - L2000-L2131 `第十九章：個股分析 — 逐項檢視單一股票條件` — 在選股雷達找到候選標的後，進入「9 - 個股分析」可以針對**單一股票**深度確認 v3 / v4 技術條件是否真的達標，以及查看完整的圖表與數值細節。
    - L2002-L2007 `功能說明` — 在選股雷達找到候選標的後，進入「9 - 個股分析」可以針對**單一股票**深度確認 v3 / v4 技術條件是否真的達標，以及查看完整的圖表與數值細節。
    - L2008-L2022 `查詢方式` — 在左側側邊欄輸入股票代號（例如 2330），選擇分析基準日後按「🔎 開始分析」。
    - L2023-L2028 `盤中即時資訊（Sponsor）` — 今日模式下，個股分析會顯示「盤中即時資訊」區塊。此區塊使用 FinMind Sponsor 的 `taiwan_stock_tick_snapshot` 即時快照，顯示最新成交價、漲跌、買賣價、累積量、開高低與資料時間。
    - L2029-L2069 `條件評分卡` — 頁面左側會依所選策略版本顯示逐項評分。v4 分為「必要條件」與「加分條件」兩組；v3 則用均線突破、量能、MACD/RSI、布林位置與主力連買來確認。
    - L2070-L2090 `圖表區` — 頁面右側顯示日K線圖，包含：
    - L2091-L2100 `技術指標數值彙整` — 在圖表下方提供一張數值摘要表，列出所有計算中間值：
    - L2101-L2109 `歷史回溯模式` — 在左側選擇**過去的日期**，系統會：
    - L2110-L2121 `Premium 摘要區（Sponsor 試用）` — 若啟用 FinMind Sponsor / Premium 功能，個股分析頁會在技術條件之外顯示 Premium 摘要。這個區塊以本機快取與頁面已載入資料為主，不會為了摘要額外批次呼叫 Premium API。
    - L2122-L2131 `注意事項` — 個股分析的日K與歷史模式讀取**本機快取**，若快取無資料會提示去「資料管理」頁面更新
  - L2132-L2215 `第二十章：FinMind Sponsor Premium 試用功能` — Sponsor Premium 功能是這套工具的「增強層」，不是核心策略本身。v3 / v4 的技術條件、掃描流程、持股監控與風控原則都可以在 Free 模式下運作。Premium 資料只用來補強三件事：
    - L2134-L2141 `功能定位` — Sponsor Premium 功能是這套工具的「增強層」，不是核心策略本身。v3 / v4 的技術條件、掃描流程、持股監控與風控原則都可以在 Free 模式下運作。Premium 資料只用來補強三件事：
    - L2142-L2150 `降級原則` — 系統採用「可關閉、可降級、可觀察」的設計：
    - L2151-L2159 `在各頁面看到什麼` — | 頁面 | Premium 相關內容 | 是否會額外呼叫 Premium API |
    - L2160-L2179 `目前資料種類與使用狀態` — | 資料類別 | FinMind dataset | 權限 | 本機快取 / 狀態 | 目前用在哪裡 |
    - L2180-L2186 `Known Issues / 後續確認` — `TaiwanStockShareholdingTransfer`（申報轉讓）目前列入管線與顯示，但實際資料來源仍需確認。若個別股票新聞有申報轉讓、FinMind 回傳沒有資料，先不要把它視為程式漏抓。
    - L2187-L2202 `分數欄位怎麼看` — 選股雷達現在保留原本的 `score` 相容性，同時拆出更清楚的欄位：
    - L2203-L2215 `試用期評估怎麼用` — 進入「選股雷達 → 歷史紀錄」，上方會看到 Premium 試用期評估。建議用法：
  - L2216-L2249 `第二十一章：問題回報 — 回報問題與功能建議` — 「問題回報」頁面用來收集使用者遇到的錯誤、操作疑問與功能建議。送出後，系統會把內容存成 Markdown 檔，放在專案根目錄的 `user_reports/` 資料夾，方便後續集中整理與處理。
    - L2218-L2221 `功能說明` — 「問題回報」頁面用來收集使用者遇到的錯誤、操作疑問與功能建議。送出後，系統會把內容存成 Markdown 檔，放在專案根目錄的 `user_reports/` 資料夾，方便後續集中整理與處理。
    - L2222-L2230 `問題回報` — 選擇「問題回報」後，需要填寫：
    - L2231-L2237 `功能新增` — 選擇「功能新增」後，需要填寫：
    - L2238-L2249 `檔案保存位置` — 每次送出會建立一個獨立 Markdown 檔，檔名包含時間、回報類型與頁面名稱，例如：
  - L2250-L2342 `第二十二章：交易計畫 — 進場前的計畫與風控自動審查` — 「沒有計畫的進場，是最貴的衝動。」
    - L2254-L2261 `功能說明` — 「交易計畫」頁面（側邊欄第 4 頁）讓你在**真正下單之前**，先以文字明確記錄這次交易的邏輯，並由系統自動執行 5 條風控規則即時審查。通過後才能送出計畫，系統將計畫放入「待執行清單」；你確認成交後點「執行」，計畫自動寫入**交易日誌**
    - L2262-L2280 `新建交易計畫` — 進入「📋 交易計畫」頁面，選擇「➕ 新建計畫」頁籤，填寫以下欄位：
    - L2281-L2298 `風控自動檢查（5 條規則）` — 填寫過程中，右側「風控自動檢查」區塊即時更新，逐條顯示通過 ✅、警告 ⚠️ 或禁止 🚫 狀態。
    - L2299-L2316 `待執行清單` — 計畫送出後，進入「⏳ 待執行清單」頁籤。每一條計畫以摺疊卡片顯示：
    - L2317-L2322 `歷史計畫` — 「📜 歷史計畫」頁籤顯示所有已執行或已取消的計畫，以表格呈現，包含 RR 比、有無警告（紅色標注）、對應日誌 ID，方便事後複盤。
    - L2323-L2342 `正確使用步驟` — ```
  - L2343-L2464 `第二十三章：事件日誌 — 決策時間線與 AI 分析匯出` — 「如果不記錄決策過程，回頭只能看到結果，看不到原因。」
    - L2347-L2365 `什麼是事件日誌` — 事件日誌是系統的**黑盒子紀錄器**，自動把以下重要事件記錄下來：
    - L2366-L2377 `為什麼需要事件日誌` — 只看交易結果，無法回答：
    - L2378-L2408 `使用事件日誌頁面` — 開啟側邊欄的「📋 事件日誌」頁面（第 11 頁）：
    - L2409-L2455 `匯出供 AI 分析` — 頁面底部提供三種匯出格式，適合不同用途：
    - L2456-L2464 `注意事項` — 事件日誌是**附加紀錄**，不影響選股雷達、持股監控、交易日誌等現有功能
  - L2465-L2531 `第二十四章：資料備份與雲端同步` — 「資料是比程式碼更難重建的東西。」
    - L2469-L2476 `為什麼備份很重要` — `srock.db` 是本工具的唯一資料儲存，包含所有持股、交易日誌、快取與設定。
    - L2477-L2490 `備份腳本` — ```
    - L2491-L2502 `備份流程` — | 步驟 | 動作 |
    - L2503-L2526 `首次部署注意事項` — 本工具使用 Google Cloud 服務帳號授權 Rclone 存取 Google Drive。
    - L2527-L2531 `.env 設定` — ```env
- L2532-L2562 `三選一（優先序）` — TELEGRAM_CHAT_ID=           # 備份專用通知頻道（推薦）
    - L2540-L2547 `排程時間` — 備份已整合進 `scheduler/jobs.py`，排程器啟動後**每週一到週五 21:30 自動執行**，無需另外設定。
    - L2548-L2559 `Telegram 推播通知` — | 情況 | 推播內容 |
    - L2560-L2562 `緊急還原` — ```bash
- L2563-L2565 `列出備份` — rclone ls gdrive: --include "srock_backup_*.db.gz"
- L2566-L2569 `下載並解壓` — rclone copyto "gdrive:srock_backup_20260427.db.gz" temp_restore.db.gz
- L2570-L2577 `替換（先備份現有）` — move srock.db srock.db.bak
- L2578-L2594 `附錄：未來功能備忘` — 目前 Streamlit App 可以在同一個 Python 程序內啟動背景預抓取工作器，對本機使用最方便，也不需要額外管理服務。
    - L2580-L2594 `Worker 獨立服務化（暫列未來考量）` — 目前 Streamlit App 可以在同一個 Python 程序內啟動背景預抓取工作器，對本機使用最方便，也不需要額外管理服務。
- L2595-L2643 `附錄：常見問題 Q&A` — **Q：選股雷達選出來的股票可以直接買嗎？**
- L2644-L3155 `附錄：名詞解釋` — | 名詞 | 解釋 |
  - L2688-L2968 `第十七章：資料管理 — 快取與背景抓取` — FinMind 免費帳號每小時只有 600 次 API 請求；Sponsor 帳號可用額度較高，目前系統會依 `user_info` 回傳額度調整，Sponsor 盤後/非交易時間可用到每小時 6000 次。全市場約 950 檔普通股，每次
    - L2690-L2695 `為什麼需要本機快取？` — FinMind 免費帳號每小時只有 600 次 API 請求；Sponsor 帳號可用額度較高，目前系統會依 `user_info` 回傳額度調整，Sponsor 盤後/非交易時間可用到每小時 6000 次。全市場約 950 檔普通股，每次
    - L2696-L2715 `背景預抓取工作器` — App 啟動後，系統會自動在背景開始下載全市場的歷史價格資料。工作器由三條並行執行緒組成：
    - L2716-L2732 `執行緒即時狀態面板` — 「資料管理」頁面的工作器狀態區新增「執行緒即時狀態」三欄面板，讓你一眼看到三條執行緒各自的即時情況：
    - L2733-L2740 `休市模式` — 連假或停市期間，法人資料不會更新。開啟「🏖️ 休市模式」可讓法人快取永不過期，避免反覆呼叫 API 浪費配額。
    - L2741-L2753 `交易日判斷與參考日期` — 資料管理頁現在會先判斷「今天是否應該有交易資料」，再決定狀態文字與進度基準：
    - L2754-L2781 `資料管理頁（6 - 資料管理）功能說明` — 進入「資料管理」頁時，系統會確認內建背景工作器是否正在執行；若工作器尚未啟動，頁面會自動啟動它，不需要使用者再手動按一次「啟動」。這是為了避免服務已上線但快取沒有自動補資料的情況。
    - L2782-L2797 `目前資料庫狀態快照（2026-04-20 08:58）` — 目前正式資料庫是專案根目錄的 `srock.db`，不是 `data/stock_data.db`。`data/stock_data.db` 是 0 bytes 舊路徑，不應用來判斷資料完整度。
    - L2798-L2811 `快取品質診斷怎麼看` — 「🔍 快取品質診斷」會把本機資料分成 5 類，幫你快速找出為什麼某些股票掃不到、回測跑不動、或資料明顯不完整：
    - L2812-L2833 `市場指數資料（TAIEX）為什麼重要` — 回測模組現在會使用 **TAIEX（台灣加權股價指數）** 當作市場基準。這筆資料主要用在：
    - L2834-L2853 `每日自動更新流程` — 每個交易日，背景工作器會自動依序完成以下階段：
    - L2854-L2873 `工作器狀態儀表板說明` — 資料管理頁的狀態卡會顯示目前所在的工作階段，讓你一眼看出系統在做什麼：
    - L2874-L2884 `為什麼法人/融資資料比 OHLCV 晚？` — 法人籌碼與融資融券的資料源（FinMind）通常在盤後數小時才發布，且發布時間每日不固定。系統採用**持續輪詢**策略應對這種不確定性：
    - L2885-L2896 `法人/融資連續確認機制` — | 機制 | 說明 |
    - L2897-L2910 `背景工作器的優先順序` — ```
    - L2911-L2927 `主力券商補完排查紀錄（2026-04-20）` — 2026-04-20 早上排查時發現，主力券商補完前一晚雖然有執行，但補完進度不合理。原因不是 API 額度不足，而是舊版欄位對應造成壞快取：
    - L2928-L2937 `失敗股票與自動略過` — 若某檔股票長期抓不到新資料，系統會自動區分：
    - L2938-L2947 `首次安裝建議流程` — 啟動 App（建議 `srock up --profile local`；傳統方式也可用 `streamlit run app.py`）
    - L2948-L2968 `Yahoo Finance 補充機制` — 系統有兩種使用 Yahoo Finance 的情境，機制不同：
  - L2969-L3155 `第十八章：回測模組 — 驗證策略歷史績效` — 回測模組讓你用歷史資料**模擬**選股策略的過去表現，驗證策略邏輯是否有效，避免用未經驗證的條件進行實際交易。
    - L2971-L2976 `功能說明` — 回測模組讓你用歷史資料**模擬**選股策略的過去表現，驗證策略邏輯是否有效，避免用未經驗證的條件進行實際交易。
    - L2977-L2983 `開始前：確認資料就緒` — 回測模組**不提供內建下載按鈕**，所有歷史資料需透過 **6 - 資料管理** 頁面補充：
    - L2984-L2996 `兩個步驟` — **步驟一：設定參數並執行**（⚙️ 設定回測參數 頁籤）
    - L2997-L3009 `資金管理參數` — 最近版本的回測不再只是「每次看到訊號就固定買 1 檔」。系統改成更接近真實交易的資金管理模型，會根據帳戶大小、剩餘現金與單筆風險限制來決定是否進場與買多少股。
    - L3010-L3027 `系統如何決定買幾股` — 回測引擎會先估算這筆交易的停損距離，再用「單筆風險預算」反推出可買股數。簡化理解如下：
    - L3028-L3050 `報告新增了哪些欄位` — 新版績效報告改成以「帳戶淨值」而不是單純百分比累乘來計算，因此你會看到更多接近實際資金管理的欄位：
    - L3051-L3100 `出場規則：移動式停損/停利` — 回測引擎使用**移動式**出場規則，比傳統固定停損更能讓利潤奔跑：
    - L3101-L3112 `績效指標解讀` — | 指標 | 健康範圍 | 說明 |
    - L3113-L3134 `AI 深度解讀` — 回測結果頁籤底部提供兩種報告輸出方式：
    - L3135-L3155 `回測的常見誤解` — **誤解一：「回測績效 80%，未來也會有 80%」**
